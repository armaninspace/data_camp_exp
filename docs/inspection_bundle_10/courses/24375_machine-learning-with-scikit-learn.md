# Machine Learning with scikit-learn (`24375`)

## Visible Curated Q/A Pairs

Q: What are apply regression techniques to predict continuous outcomes?  
A: Applying regression techniques to predict continuous outcomes involves using models to estimate numerical values, such as predicting life expectancy.

Q: What are assess model performance using advanced metrics?  
A: Assessing model performance using advanced metrics means using various evaluation measures beyond basic accuracy to better understand how well a model works.

Q: What are build and tune predictive models?  
A: Building and tuning predictive models involves creating machine learning models to make predictions and adjusting their parameters to improve performance.

Q: What are enhance model performance with preprocessing techniques?  
A: Enhancing model performance with preprocessing techniques involves preparing and transforming data before modeling to help the model perform better.

Q: What is evaluate model performance on unseen data?  
A: Evaluating model performance on unseen data means testing how well a trained model predicts outcomes for new data it has not seen before.

Q: What is optimize models with hyperparameter tuning?  
A: Optimizing models with hyperparameter tuning means adjusting the settings of a model to find the best combination for improved performance.

Q: What is solve classification problems using supervised learning?  
A: Solving classification problems using supervised learning means training a model with labeled data to categorize new data into predefined classes.

Q: What are use pipelines to streamline machine learning workflows?  
A: Using pipelines to streamline machine learning workflows means chaining together data processing and modeling steps so they can be run as a single process.

## Hidden But Correct

- Why does build and tune predictive models matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply build and tune predictive models to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't build and tune predictive models enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make build and tune predictive models fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt build and tune predictive models instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does evaluate model performance on unseen data matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply evaluate model performance on unseen data to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't evaluate model performance on unseen data enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make evaluate model performance on unseen data fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt evaluate model performance on unseen data instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does solve classification problems using supervised learning matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply solve classification problems using supervised learning to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't solve classification problems using supervised learning enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make solve classification problems using supervised learning fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt solve classification problems using supervised learning instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does apply regression techniques to predict continuous outcomes matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply apply regression techniques to predict continuous outcomes to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't apply regression techniques to predict continuous outcomes enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make apply regression techniques to predict continuous outcomes fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt apply regression techniques to predict continuous outcomes instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does assess model performance using advanced metrics matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply assess model performance using advanced metrics to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- What could I misunderstand about assess model performance using advanced metrics if I only memorize the term?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would assess model performance using advanced metrics tell me about whether my approach is working?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt assess model performance using advanced metrics instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does optimize models with hyperparameter tuning matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply optimize models with hyperparameter tuning to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't optimize models with hyperparameter tuning enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make optimize models with hyperparameter tuning fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt optimize models with hyperparameter tuning instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does enhance model performance with preprocessing techniques matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply enhance model performance with preprocessing techniques to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't enhance model performance with preprocessing techniques enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make enhance model performance with preprocessing techniques fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt enhance model performance with preprocessing techniques instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does use pipelines to streamline machine learning workflows matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply use pipelines to streamline machine learning workflows to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't use pipelines to streamline machine learning workflows enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make use pipelines to streamline machine learning workflows fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt use pipelines to streamline machine learning workflows instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false

## Coverage Warnings

- none

## Policy Summary

- validated-correct count: 48
- visible curated count: 8
- hidden correct count: 40
- hard reject count: 0
- cache entries: 0

## Scraped Course Description

### Summary

Learn how to build and tune predictive models and evaluate how well they'll
perform on unseen data.

### Overview

Classification

-In this chapter, you will be introduced to classification problems and
learn how to solve them using supervised learning techniques. And you’ll
apply what you learn to a political dataset, where you classify the party
affiliation of United States congressmen based on their voting records.

Regression

-In the previous chapter, you used image and political datasets to predict
binary and multiclass outcomes. But what if your problem requires a
continuous outcome? Regression is best suited to solving such problems. You
will learn about fundamental concepts in regression and apply them to
predict the life expectancy in a given country using Gapminder data.

Fine-tuning your model

-Having trained your model, your next task is to evaluate its performance.
In this chapter, you will learn about some of the other metrics available in
scikit-learn that will allow you to assess your model's performance in a
more nuanced manner. Next, learn to optimize your classification and
regression models using hyperparameter tuning.

Preprocessing and pipelines

-This chapter introduces pipelines, and how scikit-learn allows for
transformers and estimators to be chained together and used as a single
unit. Preprocessing techniques will be introduced as a way to enhance model
performance, and pipelines will tie together concepts from previous
chapters.

### Syllabus

1. Classification  
-In this chapter, you will be introduced to classification problems and learn how to solve them using supervised learning techniques. And you’ll apply what you learn to a political dataset, where you classify the party affiliation of United States congressmen based on their voting records.

2. Regression  
-In the previous chapter, you used image and political datasets to predict binary and multiclass outcomes. But what if your problem requires a continuous outcome? Regression is best suited to solving such problems. You will learn about fundamental concepts in regression and apply them to predict the life expectancy in a given country using Gapminder data.

3. Fine-tuning your model  
-Having trained your model, your next task is to evaluate its performance. In this chapter, you will learn about some of the other metrics available in scikit-learn that will allow you to assess your model's performance in a more nuanced manner. Next, learn to optimize your classification and regression models using hyperparameter tuning.

4. Preprocessing and pipelines  
-This chapter introduces pipelines, and how scikit-learn allows for transformers and estimators to be chained together and used as a single unit. Preprocessing techniques will be introduced as a way to enhance model performance, and pipelines will tie together concepts from previous chapters.
