# Understanding Data Engineering (`24389`)

## Visible Curated Q/A Pairs

Q: What should I compare when I use moving and processing data?  
A: You should compare the different processing techniques, the steps in the pipeline, and the use of automation, parallel, or cloud computing to ensure efficient data movement and preparation.

Q: What could I misunderstand about moving and processing data if I only memorize the term?  
A: You might misunderstand that moving and processing data is a simple or single action, when it actually involves multiple coordinated steps, automation, and sometimes advanced computing resources.

Q: What is moving and processing data?  
A: Moving and processing data means transferring raw data and preparing it for analysis by combining different processing steps, often using automation and cloud or parallel computing.

Q: How would I know when moving and processing data is not enough or not the right choice?  
A: You would know moving and processing data is not enough if the pipeline cannot handle the data volume, speed, or complexity, or if the data is not adequately prepared for analysis after processing.

Q: What should I compare when I use processing data?  
A: You should compare different processing techniques, the steps involved, and whether automation, parallel, or cloud computing is needed to handle the data efficiently.

Q: What could I misunderstand about processing data if I only memorize the term?  
A: You might misunderstand that processing data is just a single step, when it actually involves multiple techniques and steps that need to be combined and automated for effective analysis.

Q: What is processing data?  
A: Processing data involves preparing raw data for analysis by using different techniques to clean, transform, and organize it into a usable format.

Q: How would I know when processing data is not enough or not the right choice?  
A: You would know processing data is not enough if the data remains too raw or unstructured for analysis, or if the chosen techniques and automation do not meet the needs for speed, scale, or accuracy.

Q: What is storing data?  
A: Storing data means saving and organizing data in systems like databases, data lakes, or data warehouses so it can be easily accessed and managed.

Q: What is what is data engineering??  
A: Data engineering is the field focused on building and managing the systems and processes that collect, store, and prepare data for analysis, making data science possible.

## Hidden But Correct

- Why does what is data engineering? matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply what is data engineering? to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't what is data engineering? enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make what is data engineering? fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt what is data engineering? instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does storing data matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply storing data to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- What should I compare when I use storing data?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What could I misunderstand about storing data if I only memorize the term?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make storing data fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt storing data instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does processing data matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply processing data to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does moving and processing data matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply moving and processing data to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt moving and processing data instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false

## Coverage Warnings

- none

## Policy Summary

- validated-correct count: 26
- visible curated count: 10
- hidden correct count: 16
- hard reject count: 0
- cache entries: 0

## Scraped Course Description

### Summary

Discover how data engineers lay the groundwork that makes data science
possible. No coding involved!

### Overview

What is data engineering?

In this chapter, you’ll learn what data engineering is and why demand for
them is increasing. You’ll then discover where data engineering sits in
relation to the data science lifecycle, how data engineers differ from data
scientists, and have an introduction to your first complete data pipeline.

Storing data

It’s time to talk about data storage—one of the main responsibilities for a
data engineer. In this chapter, you’ll learn how data engineers manage
different data structures, work in SQL—the programming language of choice
for querying and storing data, and implement appropriate data storage
solutions with data lakes and data warehouses.

Moving and processing data

Data engineers make life easy for data scientists by preparing raw data for
analysis using different processing techniques at different steps. These
steps need to be combined to create pipelines, which is when automation
comes into play. Finally, data engineers use parallel and cloud computing to
keep pipelines flowing smoothly.

### Syllabus

1. What is data engineering?  
In this chapter, you’ll learn what data engineering is and why
demand for them is increasing. You’ll then discover where data
engineering sits in relation to the data science lifecycle, how data
engineers differ from data scientists, and have an introduction to
your first complete data pipeline.

2. Storing data  
It’s time to talk about data storage—one of the main
responsibilities for a data engineer. In this chapter, you’ll learn
how data engineers manage different data structures, work in SQL—the
programming language of choice for querying and storing data, and
implement appropriate data storage solutions with data lakes and
data warehouses.

3. Moving and processing data  
Data engineers make life easy for data scientists by preparing raw
data for analysis using different processing techniques at different
steps. These steps need to be combined to create pipelines, which is
when automation comes into play. Finally, data engineers use
parallel and cloud computing to keep pipelines flowing smoothly.
