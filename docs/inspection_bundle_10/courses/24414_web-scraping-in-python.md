# Web Scraping in Python (`24414`)

## Visible Curated Q/A Pairs

Q: Why isn't css locators, chaining, enough on its own?  
A: CSS locators and chaining alone are not enough because you may need additional tools, like Response objects, to handle scraping across multiple websites or to manage more complex scraping tasks.

Q: What is css locators, chaining,?  
A: CSS locators are a syntax for selecting elements in HTML, and chaining refers to combining CSS locators with XPath to more precisely target elements for scraping.

Q: How would I transfer css locators, chaining, to a new but related situation?  
A: You would transfer CSS locators and chaining to a new situation by applying the same techniques to select and extract data from different HTML structures, adjusting your locators and chains as needed for the new context.

Q: Why isn't css locators, chaining, and responses enough on its own?  
A: CSS locators, chaining, and responses are not enough on their own because you may need additional procedures, like spiders, to automate crawling and scraping across multiple pages.

Q: What are css locators, chaining, and responses?  
A: CSS locators are used to select HTML elements, chaining combines multiple locators for precise selection, and responses are objects in scrapy that act like selectors but provide extra tools for scraping across multiple websites.

Q: How would I transfer css locators, chaining, and responses to a new but related situation?  
A: You would transfer CSS locators, chaining, and responses to a new situation by using them to select and extract data from different websites, adapting your approach to fit the new HTML structures and using responses to manage navigation and data extraction.

Q: What is introduction to html?  
A: Introduction to HTML is learning the structure of HTML, understanding why web scraping is valuable, covering HTML basics, and getting a brief introduction to XPath notation for navigating HTML code.

Q: Why isn't spiders enough on its own?  
A: Spiders alone are not enough because they rely on the procedures and tools learned in previous chapters, such as selectors and response objects, to effectively crawl and scrape data from multiple pages.

Q: Why isn't xpaths and selectors enough on its own?  
A: XPaths and selectors alone are not enough because they are just tools for extracting data from a single HTML document; additional tools and techniques are needed to handle more complex tasks like crawling multiple pages or websites.

Q: What are xpaths and selectors?  
A: XPaths are a syntax used to navigate elements within HTML code, and selectors are tools in scrapy that use XPath to extract information from HTML documents.

Q: How would I transfer xpaths and selectors to a new but related situation?  
A: You can transfer your knowledge of XPaths and selectors to a new situation by using them to extract data from different HTML documents or websites, adapting your XPath expressions and selectors to match the new HTML structures.

## Hidden But Correct

- Why does introduction to html matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply introduction to html to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why isn't introduction to html enough on its own?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=true required_entry=false
- What would make introduction to html fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- When would I need to adapt introduction to html instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does xpaths and selectors matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply xpaths and selectors to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- What would make xpaths and selectors fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does css locators, chaining, matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply css locators, chaining, to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- What would make css locators, chaining, fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does css locators, chaining, and responses matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- How would I apply css locators, chaining, and responses to the kind of data used here?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- What would make css locators, chaining, and responses fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=true required_entry=false
- Why does spiders matter in this course?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=false required_entry=false
- What are spiders?
  delivery_class: analysis_only; reasons: ["analysis_only_low_distinctiveness"]
  anchor=false required_entry=false
- How do I use spiders in this course context?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=false required_entry=false
- What would make spiders fail in a realistic case?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=false required_entry=false
- When would I need to adapt spiders instead of applying it the same way every time?
  delivery_class: analysis_only; reasons: ["analysis_only_low_serviceability"]
  anchor=false required_entry=false

## Coverage Warnings

- none

## Policy Summary

- validated-correct count: 30
- visible curated count: 11
- hidden correct count: 19
- hard reject count: 0
- cache entries: 0

## Scraped Course Description

### Summary

Learn to retrieve and parse information from the internet using the Python
library scrapy.

### Overview

Introduction to HTML

Learn the structure of HTML. We begin by explaining why web scraping can be
a valuable addition to your data science toolbox and then delving into some
basics of HTML. We end the chapter by giving a brief introduction on XPath
notation, which is used to navigate the elements within HTML code.

XPaths and Selectors

Leverage XPath syntax to explore scrapy selectors. Both of these concepts
will move you towards being able to scrape an HTML document.

CSS Locators, Chaining, and Responses

Learn CSS Locator syntax and begin playing with the idea of chaining
together CSS Locators with XPath. We also introduce Response objects, which
behave like Selectors but give us extra tools to mobilize our scraping
efforts across multiple websites.

Spiders

Learn to create web crawlers with scrapy. These scrapy spiders will crawl
the web through multiple pages, following links to scrape each of those
pages automatically according to the procedures we've learned in the
previous chapters.

### Syllabus

1. Introduction to HTML  
Learn the structure of HTML. We begin by explaining why web scraping
can be a valuable addition to your data science toolbox and then
delving into some basics of HTML. We end the chapter by giving a
brief introduction on XPath notation, which is used to navigate the
elements within HTML code.

2. XPaths and Selectors  
Leverage XPath syntax to explore scrapy selectors. Both of these
concepts will move you towards being able to scrape an HTML
document.

3. CSS Locators, Chaining, and Responses  
Learn CSS Locator syntax and begin playing with the idea of chaining
together CSS Locators with XPath. We also introduce Response
objects, which behave like Selectors but give us extra tools to
mobilize our scraping efforts across multiple websites.

4. Spiders  
Learn to create web crawlers with scrapy. These scrapy spiders will
crawl the web through multiple pages, following links to scrape each
of those pages automatically according to the procedures we've
learned in the previous chapters.
